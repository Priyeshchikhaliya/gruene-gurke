/** Auswahl im Kontaktformular. Eigenes Modul, damit die Liste sowohl in der
 *  Server Action als auch im Client-Formular verwendet werden kann. */
export const anredeOptions = ["Frau", "Herr", "Divers"] as const;
